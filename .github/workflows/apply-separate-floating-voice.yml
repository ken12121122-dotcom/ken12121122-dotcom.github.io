name: Archived separate floating voice connector

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Separate floating voice service patch already applied.'
