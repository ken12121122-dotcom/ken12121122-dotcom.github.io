name: Build Amin Pocket GBA Universal Control (Legacy Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: echo 'Bridge 14 automatic builds are retired; use the latest Bridge workflow.'
