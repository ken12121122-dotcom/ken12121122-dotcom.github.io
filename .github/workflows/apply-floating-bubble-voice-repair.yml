name: Archived floating bubble voice repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Repair already applied; workflow intentionally disabled.'
