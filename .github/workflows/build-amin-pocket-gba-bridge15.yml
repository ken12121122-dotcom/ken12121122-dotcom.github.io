name: Build Amin Pocket GBA Bridge 15 (Legacy Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: echo 'Bridge 15 automatic builds are retired; use the latest Bridge workflow.'
