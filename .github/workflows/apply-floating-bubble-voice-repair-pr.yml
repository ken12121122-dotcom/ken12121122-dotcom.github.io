name: Archived floating bubble voice PR repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PR repair already applied; workflow intentionally disabled.'
