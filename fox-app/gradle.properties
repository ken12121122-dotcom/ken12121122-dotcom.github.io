org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
# KSP2 (K2-based) crashes processing this module's Room annotations with
# "unexpected jvm signature V" — falling back to the KSP1 pipeline as a
# targeted workaround for that specific crash, not a general downgrade.
ksp.useKSP2=false
